
// 簡化的 PDF Worker - 直接使用 CDN
console.log('PDF Worker 載入中...');

// 直接載入 CDN worker
try {
  importScripts('https://unpkg.com/pdfjs-dist@4.4.168/build/pdf.worker.min.js');
  console.log('✅ PDF Worker 載入成功');
} catch (error) {
  console.error('❌ Worker 載入失敗:', error);
}
